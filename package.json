{
  "name": "vite-plus-workspace-template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r && vp run e2e",
    "dev": "vp run website#dev",
    "e2e": "vp run website#e2e",
    "mutate": "vp exec stryker run",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@stryker-mutator/core": "catalog:",
    "@stryker-mutator/typescript-checker": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
